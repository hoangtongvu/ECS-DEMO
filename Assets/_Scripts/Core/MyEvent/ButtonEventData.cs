

namespace Core.MyEvent
{
    public struct ButtonEventData
    {
       /// <summary>
        /// Use this Default instance, no need to instantiate a new one.
        /// </summary>
        public static readonly ButtonEventData Default = new();
    }
}